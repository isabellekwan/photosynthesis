# photosynthesis

`cd photosynthesis`
 
when first cloning:

`npm install`


then run:

`npm run dev`


## api key
make .env in photosynthesis folder and put in:

`VITE_GEMINI_KEY=YOURKEYHERE`
